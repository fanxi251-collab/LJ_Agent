# 2026-07-19 修改记录

## 09:28 游客导游页沉浸式改版

Task: 将 `/visitor/guide` 改为灵山大佛实景背景的沉浸式问答页，移除游客端引用与资料上传入口，并保留会话、路线、数字人和实时语音能力。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/images/guide-lingshan-buddha.webp` | 固定保存项目现有 `seed-6.webp` 背景资源 | 确保首屏稳定展示灵山大佛，并在资源失败时由 CSS 墨绿渐变回退 |
| `frontend/src/views/GuideView.vue` | 增加背景层、内容层和可用遮罩或 Esc 关闭的历史会话抽屉，解除来源、上传和工具面板引用 | 让历史记录默认不占主内容宽度，同时从游客端移除资料工具入口 |
| `frontend/src/components/ChatMain.vue`、`ConversationTimeline.vue`、`AssistantAnswer.vue` | 增加历史按钮和四个快捷问题，移除 `show-sources`、引用按钮及专用依据区块 | 快捷提问复用既有发送流程，并保持 RAG 来源数据只用于内部能力 |
| `frontend/src/components/InlineRouteCard.vue` | 新增按消息渲染的折叠路线摘要、指标、步骤和延迟地图加载 | 没有成功路线时不占空间，用户展开后才支付地图加载成本 |
| `frontend/src/composables/useRouteMap.js`、`RoutePanel.vue` | 支持字符串 ID 或 Vue 元素引用、组件独立地图实例和显式销毁 | 防止多个路线卡共用地图或会话切换后遗留高德实例 |
| `frontend/src/guide.css`、`styles.css`、`visitor-pages.css`、`main.js` | 拆分导游专属样式，增加实景背景、磨砂内容、移动底栏和全屏抽屉，并把共享响应断点统一为 820px | 保持样式低耦合和单文件规模合理，避免平板与手机出现裁切、遮挡或横向溢出 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 将数字人舞台表面改为透明磨砂风格 | 让三种 Live2D 角色与普通模式共用实景背景，不覆盖既有角色、口型和语音实现 |
| `tests/test_frontend.py` | 增加无引用/上传契约、背景资源、路线卡生命周期和移动布局断言 | 防止游客工具入口、共享地图实例或响应式布局回归 |

Notes:
- TDD RED：沉浸式壳层、固定背景资源、独立路线地图和移动端断点测试均先确认失败，再实现对应能力。
- Frontend verification：`npm test` → 31 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换。
- Full verification：`python -m pytest -q` → 286 passed，20 条既有 openpyxl 弃用警告。
- 浏览器验收：1440×900、768×1024、390×844 三档视口均无横向溢出；历史抽屉、Esc、移动底栏、普通模式和数字人模式布局通过，控制台无错误。
- 当前工作区已有的 Live2D、实时语音和后端在途修改保持原状，本次未提交或覆盖这些改动。

## 09:40 移除导游在线状态卡片

Task: 根据浏览器标注移除导游页顶部“智能导游在线”状态卡片。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/components/ChatMain.vue` | 删除状态卡片及仅用于展示它的 `serviceState` 属性 | 精简顶部操作区，同时不改变实时连接状态或停止回答逻辑 |
| `frontend/src/views/GuideView.vue` | 停止向问答主组件传递 `serviceState` | 移除已经没有消费者的展示层接口 |
| `tests/test_frontend.py` | 增加状态卡片和展示属性不再出现的契约断言 | 防止后续界面调整重新引入该卡片 |

Notes:
- TDD RED：契约测试先因仍存在 `.status-pill` 而失败，移除组件后转为通过。
- Frontend verification：`npm test` → 31 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换。
- 浏览器验收：状态卡片和“智能导游在线”文本均为 0，顶部无横向溢出。

## 09:48 移除常规模式费用提示

Task: 根据浏览器标注删除输入框下方“常规模式仅请求文字输出，不产生语音输出费用”提示。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/components/ChatMain.vue` | 删除常规模式费用提示文本，保留发送按钮和数字人语音控制分支 | 精简输入区信息层级，不改变模式或提交逻辑 |
| `frontend/src/guide.css` | 让提示移除后的发送按钮继续右对齐，并删除失效的移动端提示隐藏规则 | 避免单个按钮因 Flex 布局回到左侧，同时清理死样式 |
| `tests/test_frontend.py` | 增加提示文本不再出现及发送按钮对齐样式的契约断言 | 防止提示或删除后的布局问题回归 |

Notes:
- TDD RED：契约测试先因提示文本仍存在而失败，删除提示并补齐对齐规则后通过。
- Frontend verification：`npm test` → 31 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换。
- 浏览器验收：提示文本数量为 0，发送按钮右边距 16px，页面无横向溢出。

## 10:30 导游页全屏摄影背景适配

Task: 使用根目录 `首页背景图.jpg` 重做 `/visitor/guide` 全屏背景，并让导航、问答控件和数字人舞台适配清透景区摄影风格。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/images/guide-home-background.jpg` | 原样复制用户提供的首页背景图 | 建立稳定前端资源，同时保留根目录原图和旧背景文件 |
| `frontend/src/App.vue` | 根据当前路由添加 `visitor-app-shell--guide` 主题类 | 让全屏背景和导航玻璃主题只在导游页生效 |
| `frontend/src/visitor-pages.css` | 增加主题色变量、Vite 可打包背景引用、桌面全屏摄影背景、移动中央宫殿焦点和专属玻璃导航 | 背景覆盖内容与导航，同时确保探索页和地图页样式不受影响 |
| `frontend/src/guide.css` | 将重墨绿蒙层改为局部光晕和底部渐变，统一顶栏、快捷问题、消息、输入框、路线卡和历史抽屉的浅玻璃风格 | 保留蓝天、宫殿和倒影细节，并维持文字与控件对比度 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 将舞台、角色激活色和状态点适配导游主题变量 | 让三个 Live2D 角色融入新背景，不改变渲染、口型或语音逻辑 |
| `tests/test_frontend.py` | 覆盖新资源字节、导游路由主题、主题变量、旧背景解除引用和移动焦点 | 防止背景资源、路由隔离和响应式构图回归 |

Notes:
- TDD RED：新增背景资源和路由主题契约先分别因文件与主题类缺失而失败，实现后转绿。
- 资源服务：CSS 使用相对路径让 Vite 将图片编译到 FastAPI 已挂载的 `/assets`，未增加后端静态路由或 API。
- Frontend verification：`npm test` → 31 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换，背景图输出为哈希资产。
- Full verification：`python -m pytest -q` → 286 passed，20 条既有 openpyxl 弃用警告。
- 浏览器验收：1520×856、768×1024、390×844 均无横向溢出；移动背景为 `56% center`，数字人和历史抽屉布局稳定；探索页、地图页不残留导游主题。
- 浏览器验收未调用问答模型或实时语音服务，因此未产生模型或语音费用。

## 10:43 数字人舞台背景完全透明

Task: 根据浏览器标注移除数字人导游舞台的卡片填充与背景模糊，让景区照片直接透出。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 将舞台背景改为完全透明并关闭背景模糊，保留角色、边框、字幕和交互控件 | 让 Live2D 数字人直接融入景区摄影背景，不影响角色与实时语音逻辑 |
| `tests/test_frontend.py` | 增加舞台无渐变、透明背景和无背景模糊的静态契约 | 防止后续样式调整重新引入舞台填充层 |

Notes:
- TDD RED/GREEN：新增契约先因旧渐变与模糊样式失败，组件调整后通过。
- Frontend verification：`npm test` → 31 passed；`python -m pytest tests/test_frontend.py -q` → 21 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换。
- 浏览器验收：1520×856 与 390×844 下舞台计算样式均为透明、无背景图和无背景模糊，且页面无横向溢出。

## 13:27 数字人舞台边框透明化

Task: 根据浏览器标注将数字人导游舞台边框改为无色。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 将舞台边框颜色改为透明，保留原有尺寸和布局占位 | 移除摄影背景上的可见框线，同时避免布局尺寸发生变化 |
| `tests/test_frontend.py` | 增加数字人舞台透明边框契约 | 防止后续主题样式重新引入可见边框 |

Notes:
- TDD RED/GREEN：新增透明边框断言先因原有主题边框失败，实现后通过。
- Frontend verification：`npm test` → 31 passed；`python -m pytest tests/test_frontend.py -q` → 21 passed。
- Production build：`npm run build` → 成功，197 个模块完成转换。
- 浏览器验收：1520×856 下四条边框计算颜色均为 `rgba(0, 0, 0, 0)`，背景继续保持透明且页面无横向溢出。

## 14:00 形象选择栏移入顶部卡片

Task: 根据浏览器标注将三位数字人导游的形象选择栏从舞台移入顶部信息卡。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/components/DigitalHumanAvatarSelector.vue` | 新增独立形象选择组件，展示三位角色、当前选中项和同步状态 | 将选择职责与 Live2D 舞台渲染解耦，便于在顶部卡片复用和响应式排版 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 移除舞台内选择栏及相关样式，仅保留角色渲染和状态展示 | 避免顶部与舞台重复出现选择入口 |
| `frontend/src/features/digital-human/index.js` | 导出新的形象选择组件 | 维持数字人功能模块的统一公共入口 |
| `frontend/src/components/ChatMain.vue` | 在数字人模式的顶部卡片中渲染形象选择器，并继续转发角色切换事件 | 保持原有角色切换数据流和实时语音接口不变 |
| `frontend/src/guide.css` | 增加顶部卡片三栏布局、平板与手机第二行布局，并阻止模式按钮文字换行 | 保证选择栏移入后各视口不拥挤、不溢出 |
| `tests/test_frontend.py` | 更新选择器归属契约并增加模式按钮不换行约束 | 防止选择栏回到舞台或顶部操作区再次换行 |

Notes:
- TDD RED/GREEN：新组件归属测试先因文件缺失失败；模式按钮约束先因缺少专属样式失败，实现后均通过。
- Frontend verification：`npm test` → 31 passed；`python -m pytest tests/test_frontend.py -q` → 21 passed。
- Production build：`npm run build` → 成功，199 个模块完成转换。
- 浏览器验收：1520×856、768×1024、390×844 均无横向溢出；选择栏位于顶部卡片且不在舞台内，平板和手机自动独占第二行；Chitose 切换状态可正常更新。
- 浏览器验收未调用问答模型或实时语音服务，因此未产生模型或语音费用。

## 16:49 数字人左移与最新回答面板

Task: 将数字人模式改为桌面左侧角色、右侧最新回答，并在窄屏上下排列，同时更新输入提示。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/components/DigitalHumanAnswerPanel.vue` | 新增最新回答卡、五种空状态文案、纯文本换行、内部滚动与接近底部时的流式跟随 | 让 AI 最新回答在数字人右侧独立、持续可读地展示 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 将舞台扩展为 `42% / 58%` 双栏并新增左侧视觉容器，`900px` 以下切换为上下布局 | 让数字人明显左移，同时避免回答面板与角色、输入区重叠 |
| `frontend/src/features/digital-human/index.js` | 导出最新回答面板 | 保持数字人功能模块统一入口 |
| `frontend/src/components/ChatMain.vue` | 将展示属性改为 `answerText` 并更新输入框示例 | 明确只传递 AI 回答，使用新的路线推荐提示 |
| `frontend/src/views/GuideView.vue` | 直接传入 `assistantTranscript` | 防止用户问题回退到数字人回答区域 |
| `tests/test_frontend.py` | 增加回答面板、左右布局、响应式、滚动和新文案契约，并迁移旧字幕断言 | 防止旧底部字幕、错误数据源或布局规则回归 |

Notes:
- TDD RED/GREEN：新回答面板测试先因组件缺失失败，实现后通过；完整测试发现并迁移一条旧 `avatarCaption` 契约。
- Frontend verification：`npm test` → 31 passed；`python -m pytest tests/test_frontend.py -q` → 22 passed。
- Production build：`npm run build` → 成功，201 个模块完成转换。
- 浏览器验收：1520×856 下角色与回答卡左右分栏且不重叠；768×1024、390×844 下上下排列，输入框和底部导航可访问，三种视口均无横向溢出，控制台无错误。
- 浏览器验收未调用真实问答或实时语音服务，因此未产生模型或语音费用。

## 19:57 湖畔诗句电影式开场

Task: 仅为游客导游入口增加方案 C“湖畔诗句”电影式开场，不改动现有导游页视觉与业务流程。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/scenic-intro/components/ScenicIntro.vue` | 新增全屏摄影开场、GSAP 入场与水波退场动画、焦点管理、图片失败回退和动画资源清理 | 用单一进入按钮建立有仪式感的第一印象，同时保证异常与无动画偏好下仍可使用 |
| `frontend/src/features/scenic-intro/composables/useGuideIntro.js` | 新增路由、会话级展示状态和完成后的查询参数清理 | 让开场每个浏览器会话默认只展示一次，并与直达问答兼容 |
| `frontend/src/features/scenic-intro/lib/introPolicy.js` | 提取导游开场显示策略与查询参数清理函数 | 将路由判断与界面动画解耦，便于独立测试和后续维护 |
| `frontend/src/features/scenic-intro/scenic-intro.css` | 新增湖畔诗句构图、响应式焦点、安全区和减少动态效果规则 | 在桌面、平板和手机中保持中央宫殿、标题与按钮可见且无横向溢出 |
| `frontend/src/features/scenic-intro/index.js` | 提供开场组件与组合式函数的模块出口 | 避免全局外壳依赖功能模块内部路径 |
| `frontend/src/App.vue` | 在全局外壳承载开场，并在展示期间对导游页添加 `inert` 与 `aria-hidden`，完成后聚焦输入框 | 不修改导游页组件即可阻止背景误操作，并保持清晰的键盘交互流程 |
| `frontend/package.json`、`frontend/package-lock.json` | 增加 GSAP `3.15.0` 依赖 | 为电影式时间线、水波扩散和退场动画提供稳定实现 |
| `frontend/tests/scenic-intro.test.js` | 新增首次进入、强制重播、问题直达和查询清理测试 | 覆盖开场出现与跳过的关键策略边界 |
| `tests/test_frontend.py` | 新增开场归属、无障碍、文案、动画清理、背景打包和不侵入导游页的静态契约 | 防止后续修改把开场耦合回导游页或破坏资源加载与降级能力 |

Notes:
- TDD RED/GREEN：策略测试先因模块缺失失败；静态契约先因组件、依赖、背景导入和动画清理缺失失败，逐项实现后通过。
- 浏览器检查发现 FastAPI 未挂载 `/images`，随后改为由 Vite 导入摄影资源；构建后的哈希资源加载成功，图片失败时仍保留湖蓝渐变回退。
- 开场规则：普通 `/visitor/guide` 首次进入显示；同一会话后续进入跳过；`?intro=1` 强制重播；非空 `q` 参数直接进入问答。
- Verification：`npm test` → 35 passed；`python -m pytest tests/test_frontend.py -q` → 23 passed；`python -m pytest -q` → 288 passed；`npm run build` → 成功，210 个模块完成转换。
- 浏览器验收：1280×720、768×1024、390×844 均无横向溢出；移动端中央宫殿保持在主要视觉区域，按钮退出后 URL 清理且输入框获得焦点。
- 浏览器验收未调用真实问答或实时语音服务，因此未产生模型或语音费用。
